{
	"type": "array",
	"items": {
		"type": "object",
		"properties": {
			"display_name": {
				"type": "string",
				"description": "The name of the widget."
			},
			"author": {
				"type": "string",
				"description": "The name of the widget author."
			},

			"path": {
				"type": "string",
				"description": "The name of the widget."
			},

			"github_link": {
				"type": "string",
				"format": "uri",
				"description": "A URL to the widgets's GitHub repository."
			},
			"discord_link": {
				"type": "string",
				"format": "uri",
				"description": "A URL to the Discord post related to the widget."
			},

			"description": {
				"type": "string",
				"description": "A short description of the widget."
			}
		},
		"required": ["display_name", "author", "path", "github_link", "discord_link", "description"],
		"additionalProperties": false
	}
}